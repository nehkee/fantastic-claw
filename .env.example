# Copy this file to .env and populate with real values
# Do NOT commit .env to version control

# OpenAI API key for GPT-4o LLM analysis
# Optional: if not set, the app will use a local fallback analysis
OPENAI_API_KEY=sk-...

# X (Twitter) API credentials for posting and webhook handling
# Optional: if not set, the /post-to-x endpoint and webhook will not function
X_CONSUMER_KEY=...
X_CONSUMER_SECRET=...
X_ACCESS_TOKEN=...
X_ACCESS_TOKEN_SECRET=...
X_BEARER_TOKEN=...

# X bot handle (used in replies)
BOT_X_HANDLE=FantasticClaw
